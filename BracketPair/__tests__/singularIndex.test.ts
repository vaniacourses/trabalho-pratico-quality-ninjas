import SingularIndex from "../src/singularIndex";
import BracketPair from "./bracketPair";
import ColorIndexes from "./IColorIndexes";
import expect for expect;

describe("singularIndex", () => {
    it("deve capturar o index da próxima posição", () => {
      const singularIndex= new SingularIndex;
      const bracketPair = new BracketPair();
       
    // Abre colchetes
        singularIndex.getOpenBrackets();

    // Captura index da próxima posição
       singularIndex.getPreviousIndex[bracketPair].push(()=> {
       });

    //Configura a cor do colchete
       singularIndex.setCurrent()
}}